This directory contains supplemental source and resources that are used by the gradle build.
The only thing it has currently, are message files used when running the webdeployment subproject, but could also
contain custom groovy classes and plugins used by the build.